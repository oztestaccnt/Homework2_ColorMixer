
@{
    ViewBag.Title = "NewStuff";

    double price = 1.5;
    int qty = ViewBag.qty;

    double total = price * qty;
}

<div class="panel panel-info">
    <div class ="panel-heading"><h3>Order Details</h3></div>
    <div class="panel-body">
        <h3>QTY: @qty</h3>
        <h3>Price: @price.ToString("C")</h3>
        <h3>Total: @total.ToString("C")</h3>

    </div>
</div>